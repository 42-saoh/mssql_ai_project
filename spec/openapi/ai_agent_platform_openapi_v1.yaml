openapi: 3.1.0
info:
  title: MSSQL Analysis Agent Platform API
  version: 0.1.0
  summary: OpenAPI contract skeleton for the MSSQL analysis, documentation, and Java/MyBatis draft-generation platform.
  description: |
    Starter API contract for the central Agent platform that analyzes MSSQL Stored Procedures
    and related database metadata, produces documentation, and creates draft Java/MyBatis
    conversion artifacts.

    This contract is intentionally skeleton-level. It defines the stable API surface and
    response shapes expected by the first implementation wave while preserving project
    policies:

    - metadata access is read-only and routed through the MSSQL Metadata MCP boundary;
    - generated documentation, Java/MyBatis code, DTO/VO/Model, and DDL are draft artifacts;
    - default P25 workflows stop at validation-complete draft output without user review UI;
    - deferred publish-like actions still require validation evidence and retained approval capability;
    - live row-data queries, automatic DDL execution, and unreviewed code application are out of scope.
servers:
  - url: http://localhost:{port}
    description: Local API/BFF server
    variables:
      port:
        default: "8000"
        description: APP_PORT resolved by Makefile or scripts/resolve_dev_ports.sh.
tags:
  - name: health
    description: Service health endpoints.
  - name: requests
    description: Work request submission endpoints.
  - name: jobs
    description: Workflow job status endpoints.
  - name: artifacts
    description: Draft artifact preview and validation endpoints.
  - name: approvals
    description: Deferred human review and approval endpoints retained outside the default P25 UI flow.
  - name: registry
    description: Prompt, template, profile, and policy registry endpoints.
  - name: metadata
    description: Read-only metadata profile and MCP catalog endpoints.
paths:
  /health:
    get:
      tags: [health]
      summary: Check API service health.
      operationId: getHealth
      responses:
        "200":
          description: API service is reachable.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthResponse"
              examples:
                ok:
                  value:
                    status: ok
                    service: api
                    version: 0.1.0
  /api/v1/requests/sp-analysis:
    post:
      tags: [requests]
      summary: Submit an MSSQL Stored Procedure analysis request.
      operationId: createStoredProcedureAnalysisRequest
      description: |
        Starts a draft-only analysis workflow for one stored procedure. The resulting job is
        expected to collect metadata through the MCP boundary, build a canonical analysis
        model, generate requested artifacts, and stop at the validation-complete draft gate.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SPAnalysisRequest"
            examples:
              spWrapper:
                value:
                  dbProfileId: plf
                  target:
                    type: PROCEDURE
                    schema: dbo
                    name: usp_OrderRequest_Select
                  outputs:
                    - SP_ANALYSIS_DOCUMENT
                    - DEPENDENCY_REPORT
                    - JAVA_MYBATIS_DRAFT
                  options:
                    includeModernizationHints: true
                    includeEvidenceRefs: true
                    useAiToolOrchestration: true
      responses:
        "202":
          description: Request accepted and workflow job created.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SubmitRequestResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
  /api/v1/jobs:
    get:
      tags: [jobs]
      summary: List recent workflow jobs.
      operationId: listJobs
      parameters:
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 20
      responses:
        "200":
          description: Recent workflow jobs.
          content:
            application/json:
              schema:
                type: object
                required: [jobs]
                properties:
                  jobs:
                    type: array
                    items:
                      $ref: "#/components/schemas/Job"
  /api/v1/jobs/{jobId}:
    get:
      tags: [jobs]
      summary: Read workflow job status.
      operationId: getJob
      parameters:
        - $ref: "#/components/parameters/JobId"
      responses:
        "200":
          description: Current job state.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Job"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/jobs/{jobId}/agent-runs:
    get:
      tags: [jobs]
      summary: List sanitized agent runtime traces for a job.
      operationId: listJobAgentRuns
      description: |
        Returns model and agent trace summaries only. Raw prompt text, SP definition text,
        and raw model response text are not exposed by this endpoint.
      parameters:
        - $ref: "#/components/parameters/JobId"
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 20
      responses:
        "200":
          description: Sanitized agent run traces for the job.
          content:
            application/json:
              schema:
                type: object
                required: [jobId, agentRuns]
                properties:
                  jobId:
                    type: string
                  agentRuns:
                    type: array
                    items:
                      $ref: "#/components/schemas/AgentRunSummary"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/jobs/{jobId}/artifacts:
    get:
      tags: [artifacts]
      summary: List draft artifacts created by a job.
      operationId: listJobArtifacts
      parameters:
        - $ref: "#/components/parameters/JobId"
      responses:
        "200":
          description: Draft artifacts for the job.
          content:
            application/json:
              schema:
                type: object
                required: [jobId, artifacts]
                properties:
                  jobId:
                    type: string
                  artifacts:
                    type: array
                    items:
                      $ref: "#/components/schemas/ArtifactSummary"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/artifacts/{artifactId}:
    get:
      tags: [artifacts]
      summary: Preview a generated draft artifact.
      operationId: getArtifact
      parameters:
        - $ref: "#/components/parameters/ArtifactId"
      responses:
        "200":
          description: Draft artifact content and evidence references.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Artifact"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/artifacts/{artifactId}/validation:
    post:
      tags: [artifacts]
      summary: Validate a draft artifact before review or approval.
      operationId: validateArtifact
      parameters:
        - $ref: "#/components/parameters/ArtifactId"
      responses:
        "200":
          description: Validation report for the artifact.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ValidationReport"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/artifacts/{artifactId}/validation/latest:
    get:
      tags: [artifacts]
      summary: Read the latest validation report without creating a validation write.
      operationId: getLatestValidationReport
      parameters:
        - $ref: "#/components/parameters/ArtifactId"
      responses:
        "200":
          description: Latest validation report for the artifact.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ValidationReport"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/artifacts/{artifactId}/approval-decisions:
    post:
      tags: [approvals]
      summary: Record a human approval decision for a draft artifact.
      operationId: createApprovalDecision
      description: |
        Records a reviewer decision. This endpoint only records review state; it does not
        deploy code, execute DDL, or mutate MSSQL business data.
      parameters:
        - $ref: "#/components/parameters/ArtifactId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ApprovalDecisionRequest"
      responses:
        "201":
          description: Approval decision recorded.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApprovalRecord"
        "400":
          $ref: "#/components/responses/BadRequest"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/metadata/db-profiles:
    get:
      tags: [metadata]
      summary: List available MSSQL metadata profiles.
      operationId: listMetadataProfiles
      description: Returns safe profile metadata only; secrets and connection strings are never returned.
      responses:
        "200":
          description: Metadata profile list.
          content:
            application/json:
              schema:
                type: object
                required: [defaultProfileId, profiles]
                properties:
                  defaultProfileId:
                    type: string
                  profiles:
                    type: array
                    items:
                      $ref: "#/components/schemas/MetadataProfile"
  /api/v1/metadata/tools:
    get:
      tags: [metadata]
      summary: List read-only MSSQL Metadata MCP tools exposed to the Agent platform.
      operationId: listMetadataTools
      responses:
        "200":
          description: MCP tool catalog summary.
          content:
            application/json:
              schema:
                type: object
                required: [tools]
                properties:
                  tools:
                    type: array
                    items:
                      $ref: "#/components/schemas/MetadataToolSummary"
  /api/v1/metadata/tools/{toolName}/invoke:
    post:
      tags: [metadata]
      summary: Invoke a public read-only MSSQL Metadata MCP dependency evidence tool.
      operationId: invokeMetadataTool
      description: |
        Invokes only the public P28 dependency-evidence allowlist through the MSSQL Metadata
        MCP registry boundary: get_dependency_closure and resolve_dependency_reference.
        The request accepts a structured arguments object only. The response is a sanitized
        metadata envelope with snapshot and evidence refs; it never returns row data, executes
        stored procedures, exposes SQL definition text, stores raw metadata, performs DDL/DML,
        or falls back from PPM to PLF.
      parameters:
        - name: toolName
          in: path
          required: true
          schema:
            type: string
            enum:
              - get_dependency_closure
              - resolve_dependency_reference
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MetadataToolInvokeRequest"
      responses:
        "200":
          description: Sanitized metadata tool invocation envelope.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MetadataToolInvokeResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "422":
          $ref: "#/components/responses/ValidationError"
        "424":
          $ref: "#/components/responses/DependencyBlocked"
        "503":
          $ref: "#/components/responses/DependencyBlocked"
  /api/v1/metadata/search:
    get:
      tags: [metadata]
      summary: Search read-only MSSQL metadata objects.
      operationId: searchMetadataObjects
      description: |
        Searches metadata object identities through the MSSQL Metadata MCP read-only boundary.
        The response is limited to object identity, profile/database context, snapshot/evidence
        references, caveats, review-required markers, and blocker codes. It never returns row
        data, executes stored procedures, exposes SQL definition text, or performs DDL/DML.
        PPM search must not fall back to PLF when PPM is unavailable.
      parameters:
        - name: dbProfileId
          in: query
          required: true
          schema:
            type: string
          examples:
            master:
              value: master
            ppm:
              value: ppm
        - name: query
          in: query
          required: true
          schema:
            type: string
            minLength: 1
          example: order
        - name: objectTypes
          in: query
          required: false
          style: form
          explode: true
          schema:
            type: array
            items:
              $ref: "#/components/schemas/MetadataSearchObjectType"
            default: [PROCEDURE, TABLE, VIEW, FUNCTION]
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 20
      responses:
        "200":
          description: Metadata object search results and blocker/caveat context.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MetadataSearchResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "422":
          $ref: "#/components/responses/ValidationError"
        "424":
          $ref: "#/components/responses/DependencyBlocked"
        "503":
          $ref: "#/components/responses/DependencyBlocked"
  /api/v1/metadata/analyze:
    post:
      tags: [metadata]
      summary: Analyze read-only MSSQL metadata with bounded AI-MCP orchestration.
      operationId: analyzeMetadata
      description: |
        Runs response-only metadata analysis. The API first builds deterministic metadata
        identity context from a query or target, then allows the LLM to plan bounded
        active/read-only MCP metadata tool calls. Tool execution stays internal to the API
        registry boundary; the public metadata tool invocation allowlist is not expanded.
        Responses contain sanitized evidence digests, deterministic fact ids, LLM insights,
        review markers, and caveats. They never include row data, raw SQL definition text,
        procedure execution output, DDL/DML controls, secrets, or raw provider traces.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MetadataAnalysisRequest"
      responses:
        "200":
          description: Response-only metadata analysis with sanitized AI-MCP evidence.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MetadataAnalysisResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "422":
          $ref: "#/components/responses/ValidationError"
        "424":
          $ref: "#/components/responses/DependencyBlocked"
        "503":
          $ref: "#/components/responses/DependencyBlocked"
  /api/v1/registry/versions:
    get:
      tags: [registry]
      summary: List active registry version bindings used by generation workflows.
      operationId: listRegistryVersions
      responses:
        "200":
          description: Registry version bindings.
          content:
            application/json:
              schema:
                type: object
                required: [versions]
                properties:
                  versions:
                    type: array
                    items:
                      $ref: "#/components/schemas/RegistryVersion"
components:
  parameters:
    JobId:
      name: jobId
      in: path
      required: true
      schema:
        type: string
      example: job_123
    ArtifactId:
      name: artifactId
      in: path
      required: true
      schema:
        type: string
      example: art_123
  responses:
    BadRequest:
      description: Invalid request payload or unsupported workflow option.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    Forbidden:
      description: The requested read-only metadata dependency is blocked by policy or access.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    NotFound:
      description: Requested resource was not found.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    ValidationError:
      description: Request validation failed.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    DependencyBlocked:
      description: Required metadata dependency is unavailable or blocked by policy.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
  schemas:
    HealthResponse:
      type: object
      required: [status, service, version]
      properties:
        status:
          type: string
          enum: [ok]
        service:
          type: string
        version:
          type: string
    TargetObject:
      type: object
      required: [type, schema, name]
      properties:
        type:
          type: string
          enum: [PROCEDURE, TABLE, VIEW, FUNCTION]
        schema:
          type: string
          examples: [dbo]
        name:
          type: string
          examples: [usp_OrderRequest_Select]
    SPAnalysisRequest:
      type: object
      required: [dbProfileId, target, outputs]
      properties:
        dbProfileId:
          type: string
          description: Metadata profile identifier, for example plf or master.
        target:
          $ref: "#/components/schemas/TargetObject"
        outputs:
          type: array
          minItems: 1
          items:
            $ref: "#/components/schemas/RequestedOutputType"
        options:
          $ref: "#/components/schemas/SPAnalysisOptions"
    SPAnalysisOptions:
      type: object
      properties:
        includeEvidenceRefs:
          type: boolean
          default: true
        includeModernizationHints:
          type: boolean
          default: true
        useLlmAnalysis:
          type: boolean
          default: true
        llmProfileId:
          type: string
          default: openai_sp_semantic_analysis
          enum:
            - openai_sp_semantic_analysis
            - openai_fast_test
        allowSpDefinitionToModel:
          type: boolean
          default: true
        useAiToolOrchestration:
          type: boolean
          default: true
    SubmitRequestResponse:
      type: object
      required: [requestId, jobId, status]
      properties:
        requestId:
          type: string
        jobId:
          type: string
        status:
          $ref: "#/components/schemas/JobStatus"
        echo:
          type: object
          description: Optional starter implementation echo of the normalized request.
    Job:
      type: object
      required: [jobId, requestId, status]
      properties:
        jobId:
          type: string
        requestId:
          type: string
        status:
          $ref: "#/components/schemas/JobStatus"
        currentStep:
          oneOf:
            - $ref: "#/components/schemas/WorkflowStepType"
            - type: "null"
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
        progress:
          type: number
          minimum: 0
          maximum: 1
        blockers:
          type: array
          items:
            $ref: "#/components/schemas/MetadataSearchBlocker"
        caveats:
          type: array
          items:
            type: string
        failureReason:
          type: string
    JobStatus:
      type: string
      description: VALIDATION_COMPLETE is the default P25 terminal draft status after validation; review and approval statuses remain for deferred compatibility.
      enum:
        - SUBMITTED
        - COLLECTING_METADATA
        - ANALYZING
        - GENERATING
        - VALIDATING
        - VALIDATION_COMPLETE
        - REVIEW_PENDING
        - APPROVED
        - REJECTED
        - PUBLISHED
        - FAILED
        - CANCELED
    WorkflowStepType:
      type: string
      enum:
        - COLLECT_METADATA
        - ANALYZE
        - GENERATE
        - VALIDATE
        - REVIEW
        - PUBLISH
    ArtifactSummary:
      type: object
      required: [artifactId, type, status]
      properties:
        artifactId:
          type: string
        jobId:
          type: string
        type:
          $ref: "#/components/schemas/ArtifactType"
        status:
          $ref: "#/components/schemas/ArtifactStatus"
        title:
          type: string
        evidenceCoverage:
          type: number
          minimum: 0
          maximum: 1
    Artifact:
      allOf:
        - $ref: "#/components/schemas/ArtifactSummary"
        - type: object
          required: [content, evidenceRefs, generatorVersion, registryRefs]
          properties:
            content:
              type: string
              description: Draft artifact body or serialized file manifest.
            evidenceRefs:
              type: array
              items:
                $ref: "#/components/schemas/EvidenceRef"
            generatorVersion:
              type: string
            registryRefs:
              type: array
              items:
                type: string
            assumptions:
              type: array
              items:
                type: string
            reviewRequired:
              type: boolean
              default: true
    ArtifactType:
      type: string
      description: Persisted draft artifact type. Request output groups map to one or more of these values.
      enum:
        - SP_ANALYSIS_DOC
        - DEPENDENCY_REPORT
        - METADATA_QUERY_RESULT
        - SCHEMA_ENRICHMENT_RESULT
        - MAPPER_XML
        - MAPPER_INTERFACE
        - SERVICE_DRAFT
        - DTO_DRAFT
        - VO_DRAFT
        - MODEL_DRAFT
        - DDL_DRAFT
        - VALIDATION_REPORT
        - APPROVAL_LOG
    RequestedOutputType:
      type: string
      description: User-facing output group requested at submission time.
      enum:
        - SP_ANALYSIS_DOCUMENT
        - DEPENDENCY_REPORT
        - TABLE_COLUMN_METADATA
        - JAVA_MYBATIS_DRAFT
        - DTO_MODEL_DRAFT
        - DDL_DRAFT
    ArtifactStatus:
      type: string
      enum:
        - DRAFT
        - VALIDATED
        - REVIEW_PENDING
        - APPROVED
        - REJECTED
        - PUBLISHED
        - ARCHIVED
    EvidenceRef:
      type: object
      required: [type, objectRef, locator]
      properties:
        type:
          type: string
          enum: [MSSQL_METADATA, STATIC_ANALYSIS, LLM_INFERENCE, POLICY, TEMPLATE, USER_INPUT]
        objectRef:
          type: string
          examples: [plf.dbo.usp_OrderRequest_Select]
        locator:
          type: string
        snapshotId:
          type: string
    ValidationReport:
      type: object
      required: [artifactId, status, checks]
      properties:
        validationReportId:
          type: string
        artifactId:
          type: string
        status:
          type: string
          enum: [PASSED, FAILED, REVIEW_REQUIRED]
        checks:
          type: array
          items:
            $ref: "#/components/schemas/ValidationCheck"
        missingEvidence:
          type: array
          items:
            type: string
        manualReviewPoints:
          type: array
          items:
            type: string
    ValidationCheck:
      type: object
      required: [ruleId, severity, result]
      properties:
        ruleId:
          type: string
        severity:
          type: string
          enum: [INFO, WARNING, ERROR, BLOCKER]
        result:
          type: string
          enum: [PASS, FAIL, REVIEW_REQUIRED]
        message:
          type: string
    ModelInvocationSummary:
      type: object
      required:
        - provider
        - model
        - modelProfileId
        - promptVersion
        - outputSchemaVersion
        - inputHash
        - promptHash
        - outputHash
        - status
      properties:
        provider:
          type: string
        model:
          type: string
        modelProfileId:
          type: string
        modelRegistryRef:
          type: string
        reasoningEffort:
          type: string
        promptVersion:
          type: string
        outputSchemaVersion:
          type: string
        inputHash:
          type: string
        promptHash:
          type: string
        outputHash:
          type: string
        status:
          type: string
          enum: [SUCCEEDED, FAILED, SKIPPED]
        tokenUsage:
          type: object
          additionalProperties:
            type: integer
        latencyMs:
          type: integer
        componentInvocations:
          type: array
          description: Sanitized per-stage model invocation summaries for staged LLM analysis.
          items:
            type: object
            additionalProperties: true
    AgentRunSummary:
      type: object
      required:
        - agentRunId
        - jobId
        - agentType
        - status
        - targetRef
        - summary
        - structuredOutput
        - modelInvocation
      properties:
        agentRunId:
          type: string
        jobId:
          type: string
        agentType:
          type: string
        status:
          type: string
          enum: [SUCCEEDED, FAILED, SKIPPED]
        targetRef:
          type: string
        summary:
          type: string
        structuredOutput:
          type: object
          additionalProperties: true
        modelInvocation:
          $ref: "#/components/schemas/ModelInvocationSummary"
        createdAt:
          type: string
          format: date-time
    ApprovalDecisionRequest:
      type: object
      required: [decision, reviewer, comment]
      properties:
        decision:
          type: string
          enum: [APPROVE, REJECT, REQUEST_CHANGES]
        reviewer:
          type: string
        comment:
          type: string
        validationReportId:
          type: string
    ApprovalRecord:
      type: object
      required: [approvalId, artifactId, decision, reviewer, decidedAt]
      properties:
        approvalId:
          type: string
        artifactId:
          type: string
        decision:
          type: string
          enum: [APPROVE, REJECT, REQUEST_CHANGES]
        reviewer:
          type: string
        comment:
          type: string
        decidedAt:
          type: string
          format: date-time
    MetadataProfile:
      type: object
      required: [id, database, readOnly]
      properties:
        id:
          type: string
        database:
          type: string
        description:
          type: string
        readOnly:
          type: boolean
          const: true
    MetadataToolSummary:
      type: object
      required: [name, description, readOnly, invokable]
      properties:
        name:
          type: string
        description:
          type: string
        readOnly:
          type: boolean
          const: true
        invokable:
          type: boolean
          description: True only for public API invocation allowlisted metadata tools.
    MetadataToolInvokeRequest:
      type: object
      required: [arguments]
      additionalProperties: false
      properties:
        arguments:
          type: object
          additionalProperties: true
          description: Structured MCP tool arguments only; free-form SQL/write command keys are rejected.
    MetadataToolInvokeResponse:
      type: object
      required: [ok, toolName, dbProfileId, snapshotId, collectedAt, evidenceRefs, data]
      additionalProperties: false
      properties:
        ok:
          type: boolean
          const: true
        toolName:
          type: string
          enum:
            - get_dependency_closure
            - resolve_dependency_reference
        dbProfileId:
          type: string
        snapshotId:
          type: string
        collectedAt:
          type: string
          format: date-time
        evidenceRefs:
          type: array
          items:
            type: object
            additionalProperties: true
        data:
          type: object
          additionalProperties: true
    MetadataSearchObjectType:
      type: string
      enum: [PROCEDURE, TABLE, VIEW, FUNCTION]
    MetadataObjectIdentity:
      type: object
      required: [schema, name, type]
      properties:
        schema:
          type: string
        name:
          type: string
        type:
          $ref: "#/components/schemas/MetadataSearchObjectType"
    MetadataSearchBlocker:
      type: object
      required: [code, message]
      properties:
        code:
          type: string
          examples:
            - PPM_MANIFEST_TEMPLATE_ONLY
            - METADATA_SEARCH_MCP_TOOL_MISSING
            - PPM_DB_NOT_FOUND
            - PPM_DB_ACCESS_DENIED
            - LIVE_METADATA_UNAVAILABLE
            - DEPENDENCY_METADATA_INCOMPLETE
            - P21_LIVE_PPM_REQUIRED
        message:
          type: string
    MetadataSearchResult:
      type: object
      required:
        - objectIdentity
        - sourceProfile
        - sourceDatabase
        - evidenceRefs
        - caveats
        - reviewRequired
        - blockers
      properties:
        objectIdentity:
          $ref: "#/components/schemas/MetadataObjectIdentity"
        sourceProfile:
          type: string
        sourceDatabase:
          type: string
        snapshotId:
          type: string
        evidenceRefs:
          type: array
          items:
            $ref: "#/components/schemas/EvidenceRef"
        caveats:
          type: array
          items:
            type: string
        reviewRequired:
          type: boolean
        blockers:
          type: array
          items:
            $ref: "#/components/schemas/MetadataSearchBlocker"
    MetadataSearchResponse:
      type: object
      required:
        - dbProfileId
        - query
        - objectTypes
        - limit
        - sourceProfile
        - sourceDatabase
        - results
        - caveats
        - reviewRequired
        - blockers
      properties:
        dbProfileId:
          type: string
        query:
          type: string
        objectTypes:
          type: array
          items:
            $ref: "#/components/schemas/MetadataSearchObjectType"
        limit:
          type: integer
          minimum: 1
          maximum: 100
        sourceProfile:
          type: string
        sourceDatabase:
          type: string
        snapshotId:
          type: string
        collectedAt:
          type: string
          format: date-time
        results:
          type: array
          items:
            $ref: "#/components/schemas/MetadataSearchResult"
        caveats:
          type: array
          items:
            type: string
        reviewRequired:
          type: boolean
        blockers:
          type: array
          items:
            $ref: "#/components/schemas/MetadataSearchBlocker"
    MetadataAnalysisOptions:
      type: object
      properties:
        useLlmAnalysis:
          type: boolean
          default: true
        useAiToolOrchestration:
          type: boolean
          default: true
        llmProfileId:
          type: string
          default: openai_sp_semantic_analysis
          enum:
            - openai_sp_semantic_analysis
            - openai_fast_test
        maxTargets:
          type: integer
          minimum: 1
          maximum: 5
          default: 3
    MetadataAnalysisRequest:
      type: object
      required: [dbProfileId]
      description: Provide exactly one of query or target.
      properties:
        dbProfileId:
          type: string
        query:
          type: string
          minLength: 1
        target:
          $ref: "#/components/schemas/MetadataObjectIdentity"
        objectTypes:
          type: array
          items:
            $ref: "#/components/schemas/MetadataSearchObjectType"
          default: [PROCEDURE, TABLE, VIEW, FUNCTION]
        options:
          $ref: "#/components/schemas/MetadataAnalysisOptions"
    MetadataAnalysisInsight:
      type: object
      required: [code, objectRef, summary, status, evidenceRefs]
      properties:
        code:
          type: string
        objectRef:
          type: string
        summary:
          type: string
        status:
          type: string
          enum: [INFERRED_DESCRIPTION, REVIEW_REQUIRED]
        evidenceRefs:
          type: array
          items:
            type: string
    MetadataObjectProfile:
      type: object
      required:
        - objectRef
        - objectType
        - columnCount
        - primaryKeyCount
        - foreignKeyCount
        - indexCount
        - constraintCount
        - descriptionCoverage
        - reviewRequired
        - evidenceRefs
        - sourceFactIds
      properties:
        objectRef:
          type: string
        objectType:
          type: string
        columnCount:
          type: integer
          minimum: 0
        primaryKeyCount:
          type: integer
          minimum: 0
        foreignKeyCount:
          type: integer
          minimum: 0
        indexCount:
          type: integer
          minimum: 0
        constraintCount:
          type: integer
          minimum: 0
        descriptionCoverage:
          type: number
          minimum: 0
          maximum: 1
        reviewRequired:
          type: boolean
        evidenceRefs:
          type: array
          items:
            type: string
        sourceFactIds:
          type: array
          items:
            type: string
    MetadataInsightGroup:
      type: object
      required: [category, insights]
      properties:
        category:
          type: string
          enum:
            - COLUMN_RISK
            - RELATIONSHIP
            - INDEX
            - CONSTRAINT
            - DOCUMENTATION_GAP
            - DTO_READINESS
            - DEPENDENCY
        insights:
          type: array
          items:
            $ref: "#/components/schemas/MetadataAnalysisInsight"
    MetadataDependencyGraphNode:
      type: object
      required: [id, objectRef, objectType, status, evidenceRefs]
      properties:
        id:
          type: string
        objectRef:
          type: string
        objectType:
          type: string
        status:
          type: string
          enum: [CONFIRMED, REVIEW_REQUIRED]
        evidenceRefs:
          type: array
          items:
            type: string
    MetadataDependencyGraphEdge:
      type: object
      required: [from, to, relationshipType, status, evidenceRefs]
      properties:
        from:
          type: string
        to:
          type: string
        relationshipType:
          type: string
        status:
          type: string
          enum: [CONFIRMED, REVIEW_REQUIRED]
        evidenceRefs:
          type: array
          items:
            type: string
    MetadataDependencyGraph:
      type: object
      required: [nodes, edges, unresolved]
      properties:
        nodes:
          type: array
          items:
            $ref: "#/components/schemas/MetadataDependencyGraphNode"
        edges:
          type: array
          items:
            $ref: "#/components/schemas/MetadataDependencyGraphEdge"
        unresolved:
          type: array
          items:
            type: object
            additionalProperties: true
    MetadataDtoReadiness:
      type: object
      required: [objectRef, status, fieldCount, reviewReasons, evidenceRefs]
      properties:
        objectRef:
          type: string
        status:
          type: string
          enum: [READY, PARTIAL, REVIEW_REQUIRED]
        fieldCount:
          type: integer
          minimum: 0
        reviewReasons:
          type: array
          items:
            type: string
        evidenceRefs:
          type: array
          items:
            type: string
    MetadataAnalysisReviewMarker:
      type: object
      required: [code, message, status, evidenceRefs]
      properties:
        code:
          type: string
        message:
          type: string
        status:
          type: string
          enum: [REVIEW_REQUIRED]
        evidenceRefs:
          type: array
          items:
            type: string
    MetadataAnalysisResponse:
      type: object
      required:
        - dbProfileId
        - mode
        - objectTypes
        - sourceProfile
        - sourceDatabase
        - targets
        - summary
        - objectInsights
        - objectProfiles
        - insightGroups
        - dependencyGraph
        - dtoReadiness
        - aiToolEvidence
        - deterministicFacts
        - reviewMarkers
        - assumptions
        - caveats
        - reviewRequired
        - blockers
        - componentInvocations
      properties:
        dbProfileId:
          type: string
        mode:
          type: string
          enum: [QUERY, TARGET]
        query:
          type: string
        target:
          $ref: "#/components/schemas/MetadataObjectIdentity"
        objectTypes:
          type: array
          items:
            $ref: "#/components/schemas/MetadataSearchObjectType"
        sourceProfile:
          type: string
        sourceDatabase:
          type: string
        snapshotId:
          type: string
        collectedAt:
          type: string
          format: date-time
        targets:
          type: array
          items:
            $ref: "#/components/schemas/MetadataSearchResult"
        summary:
          type: string
        objectInsights:
          type: array
          items:
            $ref: "#/components/schemas/MetadataAnalysisInsight"
        objectProfiles:
          type: array
          items:
            $ref: "#/components/schemas/MetadataObjectProfile"
        insightGroups:
          type: array
          items:
            $ref: "#/components/schemas/MetadataInsightGroup"
        dependencyGraph:
          $ref: "#/components/schemas/MetadataDependencyGraph"
        dtoReadiness:
          type: array
          items:
            $ref: "#/components/schemas/MetadataDtoReadiness"
        aiToolEvidence:
          type: object
          additionalProperties: true
        deterministicFacts:
          type: array
          items:
            type: object
            additionalProperties: true
        reviewMarkers:
          type: array
          items:
            $ref: "#/components/schemas/MetadataAnalysisReviewMarker"
        assumptions:
          type: array
          items:
            type: string
        caveats:
          type: array
          items:
            type: string
        reviewRequired:
          type: boolean
        blockers:
          type: array
          items:
            $ref: "#/components/schemas/MetadataSearchBlocker"
        modelInvocation:
          oneOf:
            - $ref: "#/components/schemas/ModelInvocationSummary"
            - type: "null"
        componentInvocations:
          type: array
          items:
            type: object
            additionalProperties: true
    RegistryVersion:
      type: object
      required: [registryType, version]
      properties:
        registryType:
          type: string
          enum: [PROMPT, TEMPLATE, POLICY, DB_PROFILE, GENERATOR, MODEL, SCHEMA]
        version:
          type: string
        active:
          type: boolean
    ErrorResponse:
      type: object
      required: [detail, code]
      properties:
        detail:
          type: string
        code:
          type: string
          examples:
            - DEPENDENCY_BLOCKED
            - P21_LIVE_PORTAL_REQUIRED_ENV_MISSING
            - P21_LIVE_PLF_UNAVAILABLE
            - P21_LIVE_PPM_REQUIRED
            - PPM_DB_ACCESS_DENIED
            - PPM_DB_NOT_FOUND
