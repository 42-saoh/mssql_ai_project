version: 0.1.0
service: mssqlMetadata
readOnly: true
response:
  standardData:
    required: [sourceProfile, sourceDatabase, objectIdentity, caveats, reviewRequired]
    properties:
      sourceProfile: { type: string }
      sourceDatabase: { type: string }
      objectIdentity:
        type: object
        required: [database, name, objectType]
        properties:
          database: { type: string }
          schema: { type: string }
          name: { type: string }
          objectType: { type: string }
      caveats:
        type: array
        items: { type: string }
      reviewRequired: { type: boolean }
  success:
    required: [ok, toolName, dbProfileId, snapshotId, collectedAt, evidenceRefs, data]
    properties:
      ok: { type: boolean }
      toolName: { type: string }
      dbProfileId: { type: string }
      snapshotId: { type: string }
      collectedAt: { type: string, format: date-time }
      evidenceRefs:
        type: array
        items:
          type: object
          required: [id, source, path]
          properties:
            id: { type: string }
            source: { type: string }
            path: { type: string }
            objectType: { type: string }
            objectName: { type: string }
      data: { type: object }
  error:
    required: [ok, toolName, error]
    properties:
      ok: { type: boolean }
      toolName: { type: string }
      dbProfileId: { type: string }
      snapshotId: { type: string }
      collectedAt: { type: string, format: date-time }
      evidenceRefs:
        type: array
        items: { type: object }
      error:
        type: object
        required: [code, message]
        properties:
          code: { type: string }
          message: { type: string }
          details:
            type: object
            description: Safe diagnostic fields only; never SQL text, credentials, row data, or connection strings.
            properties:
              toolName: { type: string }
              dbProfileId: { type: string }
              database: { type: string }
              missingSettingsCount: { type: integer }
              timeoutSeconds: { type: integer }
              attempt: { type: integer }
              errorClass: { type: string }
errorCodes:
  - UNKNOWN_TOOL
  - INVALID_ARGUMENTS
  - PROFILE_NOT_FOUND
  - OBJECT_NOT_FOUND
  - READ_ONLY_VIOLATION
  - LIVE_METADATA_UNAVAILABLE
  - PPM_DB_NOT_FOUND
  - PPM_DB_ACCESS_DENIED
  - METADATA_READ_ONLY_PERMISSION_INSUFFICIENT
  - SP_DEFINITION_ACCESS_DENIED
  - DEPENDENCY_METADATA_INCOMPLETE
  - INTERNAL_ERROR
tools:
  - name: check_database_exists
    active: true
    readOnly: true
    description: Return database existence and catalog accessibility metadata.
    input:
      type: object
      required: [dbProfileId]
      properties:
        dbProfileId: { type: string }
        databaseName: { type: string }

  - name: list_procedures
    active: true
    readOnly: true
    description: Return procedure inventory metadata for object selection.
    input:
      type: object
      required: [dbProfileId]
      properties:
        dbProfileId: { type: string }
        schema: { type: string }
        topK: { type: integer, default: 100 }

  - name: list_tables
    active: true
    readOnly: true
    description: Return table inventory metadata for object selection.
    input:
      type: object
      required: [dbProfileId]
      properties:
        dbProfileId: { type: string }
        schema: { type: string }
        topK: { type: integer, default: 100 }

  - name: list_views
    active: true
    readOnly: true
    description: Return view identity and definition availability metadata.
    input:
      type: object
      required: [dbProfileId]
      properties:
        dbProfileId: { type: string }
        schema: { type: string }
        topK: { type: integer, default: 100 }

  - name: list_functions
    active: true
    readOnly: true
    description: Return function identity and definition availability metadata.
    input:
      type: object
      required: [dbProfileId]
      properties:
        dbProfileId: { type: string }
        schema: { type: string }
        topK: { type: integer, default: 100 }

  - name: get_procedure_definition
    active: true
    readOnly: true
    description: Return stored procedure definition metadata.
    input:
      type: object
      required: [dbProfileId, schema, procedureName]
      properties:
        dbProfileId: { type: string }
        schema: { type: string }
        procedureName: { type: string }
        snapshotMode: { type: string, enum: [LATEST, PINNED] }
        snapshotId: { type: string }

  - name: get_procedure_parameters
    active: true
    readOnly: true
    description: Return stored procedure parameter metadata.
    input:
      type: object
      required: [dbProfileId, schema, procedureName]
      properties:
        dbProfileId: { type: string }
        schema: { type: string }
        procedureName: { type: string }
        snapshotMode: { type: string, enum: [LATEST, PINNED] }
        snapshotId: { type: string }

  - name: get_procedure_dependencies
    active: true
    readOnly: true
    description: Return stored procedure dependency metadata.
    input:
      type: object
      required: [dbProfileId, schema, procedureName]
      properties:
        dbProfileId: { type: string }
        schema: { type: string }
        procedureName: { type: string }
        snapshotMode: { type: string, enum: [LATEST, PINNED] }
        snapshotId: { type: string }

  - name: get_related_db_objects
    active: true
    readOnly: true
    description: Return related table/view/function objects.
    input:
      type: object
      required: [dbProfileId, schema, objectName, objectType]
      properties:
        dbProfileId: { type: string }
        schema: { type: string }
        objectName: { type: string }
        objectType: { type: string, enum: [PROCEDURE, TABLE, VIEW, FUNCTION] }
        topK: { type: integer, default: 20 }

  - name: get_table_schema
    active: true
    readOnly: true
    description: Return columns and data types for a table.
    input:
      type: object
      required: [dbProfileId, schema, tableName]
      properties:
        dbProfileId: { type: string }
        schema: { type: string }
        tableName: { type: string }

  - name: get_table_constraints
    active: true
    readOnly: true
    description: Return PK/FK/UQ/CHECK constraints for a table.
    input:
      type: object
      required: [dbProfileId, schema, tableName]
      properties:
        dbProfileId: { type: string }
        schema: { type: string }
        tableName: { type: string }

  - name: get_table_indexes
    active: true
    readOnly: true
    description: Return index metadata for a table.
    input:
      type: object
      required: [dbProfileId, schema, tableName]
      properties:
        dbProfileId: { type: string }
        schema: { type: string }
        tableName: { type: string }

  - name: get_extended_properties
    active: true
    readOnly: true
    description: Return descriptions and logical names.
    input:
      type: object
      required: [dbProfileId, schema, objectName]
      properties:
        dbProfileId: { type: string }
        schema: { type: string }
        objectName: { type: string }
        objectType: { type: string, enum: [PROCEDURE, TABLE, COLUMN, VIEW, FUNCTION] }

  - name: get_view_definition
    active: true
    readOnly: true
    description: Return view definition metadata.
    input:
      type: object
      required: [dbProfileId, schema, viewName]
      properties:
        dbProfileId: { type: string }
        schema: { type: string }
        viewName: { type: string }
        snapshotMode: { type: string, enum: [LATEST, PINNED] }
        snapshotId: { type: string }

  - name: get_function_definition
    active: true
    readOnly: true
    description: Return function definition metadata.
    input:
      type: object
      required: [dbProfileId, schema, functionName]
      properties:
        dbProfileId: { type: string }
        schema: { type: string }
        functionName: { type: string }
        snapshotMode: { type: string, enum: [LATEST, PINNED] }
        snapshotId: { type: string }

  - name: search_tables
    active: true
    readOnly: true
    description: Search tables by name, description, or column structure.
    input:
      type: object
      required: [dbProfileId]
      properties:
        dbProfileId: { type: string }
        physicalName: { type: string }
        logicalName: { type: string }
        description: { type: string }
        columns:
          type: array
          items: { type: string }
        topK: { type: integer, default: 5 }

  - name: search_columns
    active: true
    readOnly: true
    description: Search columns by physical/logical name or description.
    input:
      type: object
      required: [dbProfileId]
      properties:
        dbProfileId: { type: string }
        physicalName: { type: string }
        logicalName: { type: string }
        description: { type: string }
        tableName: { type: string }
        topK: { type: integer, default: 5 }

  - name: find_similar_tables
    active: true
    readOnly: true
    description: Recommend similar tables using metadata signals.
    input:
      type: object
      required: [dbProfileId, columns]
      properties:
        dbProfileId: { type: string }
        description: { type: string }
        columns:
          type: array
          items:
            type: object
            required: [name]
            properties:
              name: { type: string }
              type: { type: string }
        topK: { type: integer, default: 5 }
