CREATE PROCEDURE dbo.usp_OrderTemp
AS
BEGIN
    CREATE TABLE #TMP_ORDER (
        ORDER_ID BIGINT,
        CUSTOMER_ID BIGINT
    );

    INSERT INTO #TMP_ORDER (ORDER_ID, CUSTOMER_ID)
    SELECT ORDER_ID, CUSTOMER_ID
    FROM dbo.TB_ORDER;

    SELECT * FROM #TMP_ORDER;
END
