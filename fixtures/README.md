# fixtures

representative SP / metadata / schema-search fixtures 를 둔다.

현재 하위 분류:
- `fixtures/mssql` — representative stored procedure fixture
- `fixtures/metadata` — schema search / metadata fixture
- `fixtures/eval` — eval fixture 자리

