# packages/templates

artifact template, prompt template, naming/package 규칙 자료를 둔다.

현재 artifact registry:

- `artifacts/java_mybatis_registry.yaml` — Java/MyBatis draft template id, version, output role, SQL risk marker, draft-quality caveat 기준.

Naming/package 자체 규칙은 `spec/policy/project_ai_java_mybatis_generation_policy.yaml` 을 read-only source of truth 로 사용한다.
