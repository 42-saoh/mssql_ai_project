# AGENTS.md

이 저장소에서 Codex가 따라야 하는 기본 작업 규칙이다.

## 미션

이 저장소의 목적은 MSSQL 분석·문서화·전환코드 생성 Agent 플랫폼을 설계·구현·검증하는 것이다.  
모든 작업은 **근거 기반**, **검증 우선**, **승인 게이트**, **작은 변경 단위**를 지켜야 한다.

## 먼저 읽을 문서

- 항상 `PROJECT.md`
- 아키텍처 / API / DB / 계약 변경 작업이면 `ARCHITECTURE.md`
- 도구 / 명령 / MCP / 환경 관련 작업이면 `TOOLS.md`
- 보안 / 승인 / 금지 행위 판단이 필요하면 `POLICY.md`
- 완료 기준 또는 테스트 설계가 필요하면 `EVAL_SPEC.md`
- 새로운 작업 브리프를 만들거나 실행할 때는 `TASK_TEMPLATE.md`

## 기본 작업 방식

1. 먼저 범위와 산출물을 명확히 잡는다.
2. 변경은 가능한 한 작은 슬라이스로 나눈다.
3. 파서, 규칙, 검증 같은 핵심 로직은 LLM 추론보다 결정론적 구현을 우선한다.
4. 메타데이터 사실 수집은 `services/mssql-mcp` 경계를 통해서만 수행한다.
5. 실제 데이터 조회/수정, 자동 DDL 실행, 자동 배포, 무검증 자동 반영은 금지한다.
6. 아키텍처/API/DDL/정책이 바뀌면 문서도 같은 작업 안에서 갱신한다.
7. 없는 명령을 상상하지 말고 확인한다. 다만 저장소 부트스트랩 단계라면 표준 명령을 새로 만들고 문서에 반영한다.
8. 테스트가 없다면 가장 좁은 범위의 테스트 또는 계약 검증을 추가한다.
9. 결과 보고에는 바뀐 파일, 검증 결과, 남은 리스크를 포함한다.

## 변경 원칙

- 관련 없는 파일은 건드리지 않는다.
- 거대한 리팩터링보다 작은 단계별 변경을 선호한다.
- 숨겨진 전역 상태, 불명확한 매직 문자열, 하드코딩된 환경값을 만들지 않는다.
- 프롬프트/템플릿/규칙은 버전 가능하고 비교 가능한 형태로 저장한다.
- 근거가 약한 추론 결과는 `REVIEW_REQUIRED` 또는 동등한 표시를 남긴다.

## 완료 기준

작업은 아래를 만족할 때 완료로 본다.

- 요구 산출물이 생성되었다.
- 정책 위반이 없다.
- 관련 테스트/검증이 수행되었다.
- 문서와 코드가 서로 모순되지 않는다.
- 변경 사유와 영향 범위가 설명 가능하다.

## 서브에이전트 라우팅 가이드

명시적으로 요청받은 경우에만 서브에이전트를 사용한다.

- `architect` : 시스템 구조, 경계, 계약, ADR
- `platform_worker` : API/BFF, workflow, validation, storage 구현
- `mcp_engineer` : MSSQL Metadata MCP 서버, 메타데이터 계약, 읽기 전용 경계
- `template_engineer` : Canonical 모델, 프롬프트/템플릿, 생성기
- `reviewer` : correctness/security/test/docs drift 리뷰
- `docs_curator` : 문서 동기화, 사용 가이드, 운영 문서

## 스킬 사용 가이드

저장소 루트의 `.agents/skills` 아래 스킬을 사용한다.

- `repo-bootstrap` : 저장소 초기 구조와 문서/설정 세팅
- `contract-to-code` : 설계/계약을 구현 슬라이스로 변환
- `mcp-tooling-design` : MCP 도구/스키마/에러 모델 설계
- `eval-fixture-authoring` : fixture/eval/rubric 설계
- `quality-gate-review` : 품질 게이트/리뷰/승인 전 점검
- `docs-sync` : 문서 동기화
- `context7-docs` : 최신 프레임워크/라이브러리 문서 확인
- `browser-automation-smoke` : 비파괴적 로컬 UI smoke 검증

## 응답 형식

작업 완료 보고에는 최소 아래를 포함한다.

- 무엇을 바꿨는지
- 어떤 파일이 바뀌었는지
- 무엇으로 검증했는지
- 아직 남은 리스크나 TODO

## 외부 DB / 스키마 변경 추가 규칙

- 저장소는 로컬 DB 컨테이너 lifecycle 을 관리하지 않는다.
- DB 스키마 변경이 필요하면 `db/schema/` 아래에 버전 업 SQL 파일만 추가한다.
- `db/schema/` 의 SQL 은 설계/검토/수동 적용 대상이며, Codex 가 자동 실행하거나 적용하지 않는다.
- 테스트가 외부 DB 를 필요로 하더라도, 해당 DB 의 기동/중지는 저장소 밖에서 관리한다고 가정한다.

## 테스트 실행 추가 규칙

- 기본 검증 명령은 Makefile 의 도커 기반 테스트 명령을 우선 사용한다.
- 파이썬 계열 테스트는 `make test` 와 `PYTEST_ARGS` 조합을 기본 인터페이스로 사용한다.
- 전용 UI 테스트가 아직 없으면 `make test-web-smoke` 또는 승인된 Playwright MCP smoke 로 최소 검증을 수행하고, 테스트 공백을 명시적으로 보고한다.
